
import React, { memo } from 'react';

import {
  NavBarWrapper
} from "./style";

export default memo(function NavBar(props) {
  return (
    <NavBarWrapper>
      <div className="bar">
      </div>
    </NavBarWrapper>
  )
})